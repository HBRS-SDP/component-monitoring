name: ros_master_monitor
description: ROS master running
mappings:
    - mapping:
        inputs: []
        outputs:
            - output:
                name: master_running
                type: bool
