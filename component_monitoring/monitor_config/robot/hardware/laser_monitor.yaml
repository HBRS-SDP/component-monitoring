# name: string | required
# submonitors: list<string> | optional
# dependencies: list<string> | optional
#

name: laser_monitor
component_name: laser
modes: [laser/device.yaml]
#modes: [laser/device.yaml, laser/heartbeat.yaml, laser/functional.yaml]
dependencies: []
